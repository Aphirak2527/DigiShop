{
  "installCommand": "corepack enable && corepack prepare pnpm@10.2.0 --activate && cd ../../ && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
