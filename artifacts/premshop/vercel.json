{
  "installCommand": "npm install -g pnpm && cd ../../ && pnpm install --frozen-lockfile=false",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
