{
  "installCommand": "npm install --no-package-lock",
  "buildCommand": "cd artifacts/premshop && npm install --no-package-lock && npm run build",
  "outputDirectory": "artifacts/premshop/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
